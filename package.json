{
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "docs": "vitepress --root docs",
    "docs:build": "vitepress build --root docs"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5",
    "typescript": "^4.0.2",
    "vitepress": "^0.6.0",
    "vite": "^1.0.0-rc.4"
  }
}
